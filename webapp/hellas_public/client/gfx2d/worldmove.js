

export function start_move_hero() {
  // remove previous arrow
  // check if area already contains a hero

  // create arrow
  // calc time to move
  // add server request (area_id, coord, hero_id)
}

export function end_move_hero() {
  // add server request (hero_id)
  // if success:

  // delete arrow
  // move hero coords
  // reassign area_id

  // if battle:
  // handle number changes
}


// todo: add same for warships (no area assignment)